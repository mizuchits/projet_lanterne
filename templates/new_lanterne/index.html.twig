{% extends 'base.html.twig' %}

{% block title %}Ajouter une Lanterne{% endblock %}

{% block body %}
<div class="container">
	<div class="form-container">
		<h2>Ajouter une lanterne</h2>
		{{form_start(Newlanterne)}}
		{{form_widget(Newlanterne)}}
		<button type="submit" class="btn">Ajouter</button>
		{{form_end(Newlanterne) }}
	</div>
</div>
{% endblock %}
